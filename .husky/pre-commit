# Run staged linters and checks
# Run staged linters and checks
pnpm exec lint-staged

# Run typecheck on affected packages (only checks packages with staged changes)
pnpm run typecheck --filter="[HEAD]"